<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Game1.ChangeSongActivity"></activity>
        <activity android:name=".Game1.ChooseBallActivity" />
        <activity android:name=".Game1.ShopActivity" />
        <activity android:name=".Game1.CustomizeActivity" />
        <activity android:name=".Game1.BallJumperActivity" />
        <activity android:name=".Game3Activity" />
        <activity android:name=".Game2Activity" />
        <activity android:name=".Game1.Game1Activity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>